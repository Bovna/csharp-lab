@model Vjezba.Web.ViewModels.TicketBuilderMovieListViewModel
@{
    ViewData["Title"] = "Brza kupnja - Filmovi";
}

<div class="tb-cinema tb-page animate-fade-in-up">
    <div class="tb-header">
        <h1>@Model.CinemaName</h1>
        <p>Korak 2/4: odaberi film</p>
    </div>

    <div class="tb-step-line animate-fade-in-up" aria-label="Koraci brze kupnje">
        <a class="tb-step is-link" asp-controller="TicketBuilder" asp-action="Index">1 Kino</a>
        <span class="tb-step active" aria-current="step">2 Film</span>
        <span class="tb-step is-disabled" aria-disabled="true">3 Projekcija</span>
        <span class="tb-step is-disabled" aria-disabled="true">4 Sjedalo</span>
    </div>

    @if (!Model.Movies.Any())
    {
        <p>Nema dostupnih filmova za ovo kino.</p>
    }
    else
    {
        <div class="tb-grid-cards animate-fade-in-up">
            @{
                int delayMs = 100;
            }
            @foreach (var movie in Model.Movies)
            {
                <a class="tb-card-link animate-fade-in-up" style="animation-delay: @(delayMs)ms;" asp-controller="TicketBuilder"
                   asp-action="Screenings" asp-route-cinemaId="@Model.CinemaId" asp-route-movieId="@movie.Id">
                    <strong>@movie.Title</strong>
                    <span>@movie.Genre | @movie.DurationMinutes min</span>
                    <small>@movie.Language | @movie.AgeRating</small>
                </a>
                delayMs += 50;
            }
        </div>
    }
</div>
