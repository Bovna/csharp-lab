@model Vjezba.Web.ViewModels.SeatFormViewModel

@{
    ViewData["Title"] = "Dodaj sjedalo";
    ViewData["IsEdit"] = false;
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8 entity-form">
            <div class="cinema-card animate-fade-in-up">
                <div class="cinema-card-header">Dodaj novo sjedalo</div>
                <div class="cinema-card-body">
                    <partial name="SeatForm" model="Model" view-data="ViewData" />
                    </div>
                </div>
            </div>
        </div>
    </div>